name: Runner Probe

on:
  push:
    branches:
      - feat/native-transfer-compare

jobs:
  probe:
    runs-on: ubuntu-22.04
    timeout-minutes: 2
    steps:
      - name: Runner alive
        run: echo "runner-ok"
